import React, { Component } from "react";
import Nav from "./Nav";

class Board extends Component {
  state = {};
  render() {
    return (
      <div className="board">
        <Nav />
        board component
      </div>
    );
  }
}

export default Board;
